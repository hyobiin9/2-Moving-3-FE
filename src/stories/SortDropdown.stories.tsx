import SortDropdown from "@/components/dropdown/SortDropdown";

export default {
  title: "Components/Dropdown/SortDropdown",
  component: SortDropdown,
};

const Template = () => <SortDropdown />;

export const Default = Template.bind({});
